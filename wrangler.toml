# Cloudflare Pages：连接仓库后在控制台填写
# 构建命令：npm run build
# 构建输出目录：dist
# 或使用 CLI：npx wrangler pages deploy dist --project-name=json-toolkit

name = "json-toolkit"
compatibility_date = "2025-05-01"
pages_build_output_dir = "dist"
